pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
imbalanced-learn>=0.11
matplotlib>=3.7
seaborn>=0.12
streamlit>=1.33
joblib>=1.3
plotly>=5.18
scipy>=1.11
statsmodels>=0.14
prophet>=1.1     # optional — Challenge 1 classical baseline
torch>=2.0       # optional — Challenge 1 LSTM baseline (multivariate)
shap>=0.45       # optional — Phase 5 feature attribution
pmdarima>=2.0    # optional — auto_arima per-group order selection
catboost>=1.2          # optional — Tier 1 native-categorical GBM benchmark
statsforecast>=1.7     # optional — Tier 1 AutoTheta / M-comp classical baselines
neuralforecast>=1.7    # optional — Tier 2 modern DL forecasters (NBEATS, NHITS, DLinear, DeepAR, TFT)
